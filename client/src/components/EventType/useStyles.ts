import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    flexGrow: 1,
  },
  title: {
    flexGrow: 1,
    marginTop: '13px',
    marginLeft: '15px',
  },
  left: {
    float: 'left',
  },
});

export default useStyles;
